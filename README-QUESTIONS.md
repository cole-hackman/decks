# README questions — needs Cole's input

1. **Problem provenance.** One line on how this started — presumably your
   own Rekordbox library and a specific chore (which one?). The draft has a
   `<!-- TODO: verify -->` where it belongs.

2. **Waveform state.** ADR-0008 (May 2026) made the track-inspector
   waveform a labeled synthetic preview, but the feature docs now claim a
   native ANLZ color waveform in track detail. If any surface still shows
   the synthetic preview, say which, so the README doesn't overclaim; the
   draft currently just says ANLZ waveforms are parsed.
